#!/bin/bash
# Google Drive to Markdown exporter wrapper
# Usage: gdrive_to_md <google_drive_folder_url>

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

python3 "$PROJECT_ROOT/utils/gdrive_to_md.py" "$@"
